# Лабораторная работа 3: Программа как коллекция команд

## Описание
Реализация программы для виртуальной машины в виде отдельного класса с поддержкой анализа и итерации. Проект демонстрирует работу с коллекциями, Stream API и паттерном Iterable.

## Функциональность

### 🔧 Основные возможности
- **Программа как коллекция** - класс `Program` для управления командами
- **Итерация по командам** - реализация интерфейса `Iterable<Command>`
- **Анализ программы** - статистика использования инструкций и памяти
- **Stream API** - использование современных возможностей Java

### 📊 Аналитические функции
```java
Program prog = new Program();
// Добавление команд...
prog.mostPopularInstruction();      // Наиболее частая инструкция
prog.getMemoryAddressRange();       // Диапазон адресов памяти
prog.getInstructionsByFrequency();  // Инструкции по частоте использования
```
# Реализованные требования
✅ Класс Program как отдельная сущность

✅ Добавление инструкций через add()

✅ Реализация Iterable<Command>

✅ Анализ через Collections/Stream API:

* Наиболее частая инструкция

* Диапазон адресов памяти

* Сортировка по частоте использования

